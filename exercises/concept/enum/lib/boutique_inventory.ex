defmodule BoutiqueInventory do
  def sort_by_price(inventory) do
    raise "Please implement the sort_by_price/1 function"
  end

  def with_missing_price(inventory) do
    raise "Please implement the with_missing_price/1 function"
  end

  def increase_quantity(item, count) do
    raise "Please implement the increase_quantity/2 function"
  end

  def total_quantity(item) do
    raise "Please implement the total_quantity/1 function"
  end
end
